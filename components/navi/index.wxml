<view class='container'>
  <image bind:tap="onLeft" class='icon' src="{{latest?disleft:left}}"/>
  <text class='title'>{{title}}</text>
  <image bind:tap="onRight" class='icon' src="{{first?disright:right}}"/>
</view>
